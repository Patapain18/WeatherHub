// Modèle du fichier Cles.swift, qui n'est pas dans git.
// À copier sous le nom Cles.swift (dans ce même dossier — Xcode le voit
// déjà, il attend juste le fichier), puis coller sa clé.
// Ce fichier-ci n'est pas compilé : il n'est pas dans la cible Xcode.

enum Cles {
    /// Clé OpenWeather (géocodage, qualité de l'air, tuiles de la carte).
    /// Plan gratuit : https://openweathermap.org/api — 1 000 appels/jour.
    static let openWeather = "VOTRE_CLE_OPENWEATHER"
}
